{
  "name": "artify-js-starter",
  "version": "2.0.2",
  "description": "A starter kit for building web applications in Javscript",
  "scripts": {
    "dev": "npx vite serve --host",
    "build": "npx vite build --emptyOutDir",
    "preview": "npx vite preview --host"
  },
  "license": "AFL-3.0",
  "devDependencies": {
    "msw": "^0.47.4",
    "vite": "^3.2.3"
  },
  "msw": {
    "workerDirectory": "src/public"
  }
}
